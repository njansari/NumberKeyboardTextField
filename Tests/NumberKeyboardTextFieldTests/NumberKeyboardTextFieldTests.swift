import XCTest
@testable import NumberKeyboardTextField

final class NumberKeyboardTextFieldTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(NumberKeyboardTextField().text, "Hello, World!")
    }
}
